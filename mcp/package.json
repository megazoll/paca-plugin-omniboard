{
  "name": "@paca-ai/omniboard-plugin-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run vite build",
    "dev": "bun run vite build --watch",
    "typecheck": "bun run tsc --noEmit"
  },
  "dependencies": {
    "@paca-ai/plugin-sdk-mcp": "^0.1.0"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vite": "^6.0.0"
  }
}
